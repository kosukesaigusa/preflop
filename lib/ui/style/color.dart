import 'package:flutter/material.dart';

/// アプリケーションで利用する色一覧。
abstract interface class AppColor {
  /// ダークネイビー。
  static const darkNavy = Color(0xFF1A1A2E);

  /// ダークブルーグレー。
  static const darkBlueGrey = Color(0xFF2C2C44);

  /// グレー。
  static const grey = Color(0xFF404040);

  /// ライトグレー。
  static const lightGrey = Color(0xFFE4E4E7);

  /// ミディアムグレー。
  static const mediumGrey = Color(0xFFA1A1AA);

  /// ゴールド。
  static const gold = Color(0xFFE5B94E);

  /// グリーン。
  static const green = Color(0xFF22C55E);

  /// イエロー。
  static const yellow = Color(0xFFFFD700);

  /// ブルー。
  static const blue = Color(0xFF3B82F6);

  /// レッド。
  static const red = Color(0xFFEF4444);

  /// 白。
  static const white = Color(0xFFFFFFFF);

  /// 黒。
  static const black = Color(0xFF000000);

  /// 透明。
  static const transparent = Color(0x00000000);
}
